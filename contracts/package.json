{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Cairo contracts for OneSat",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "dev": "tsc --watch",
    "deploy": "ts-node scripts/deploy.ts",
    "deploy:sepolia": "ts-node scripts/deploy.ts sepolia",
    "deploy:mainnet": "ts-node scripts/deploy.ts mainnet",
    "deploy:devnet": "ts-node scripts/deploy.ts devnet"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.17",
    "dotenv": "^16.3.1",
    "jest": "^29.7.0",
    "starknet": "^6.4.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
